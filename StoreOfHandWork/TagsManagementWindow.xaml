<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="StoreOfHandWork.TagsManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StoreOfHandWork"
        mc:Ignorable="d"
        Title="Управление тегами" Height="500" Width="700" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980b9"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#bdc3c7"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Background="#3498db" Grid.Row="0">
            <TextBlock Text="Управление тегами" 
                     Foreground="White"
                     FontSize="22"
                     FontWeight="Bold"
                     Margin="20,15"
                     HorizontalAlignment="Left"/>
        </Border>

        <!-- Основное содержимое -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <!-- Список тегов -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Список тегов" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                
                <DataGrid x:Name="TagsDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                          SelectionMode="Single" SelectionChanged="TagsDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Кол-во товаров" Binding="{Binding ProductCount}" Width="120"/>
                        <DataGridTemplateColumn Header="Действия" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="✏️" ToolTip="Редактировать" Click="EditTag_Click" 
                                                Tag="{Binding Id}" Width="25" Height="25" Margin="2,0"/>
                                        <Button Content="❌" ToolTip="Удалить" Click="DeleteTag_Click" 
                                                Tag="{Binding Id}" Width="25" Height="25" Margin="2,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- Форма добавления/редактирования тега -->
            <Border Grid.Column="1" BorderBrush="#e0e0e0" BorderThickness="1" Margin="15,0,0,0" Padding="15" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="FormTitleTextBlock" Grid.Row="0" Text="Добавить новый тег" 
                              FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>

                    <TextBlock Grid.Row="1" Text="Название тега:" Margin="0,0,0,5"/>
                    <TextBox x:Name="TagNameTextBox" Grid.Row="2" Padding="5" Margin="0,0,0,15"/>

                    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="CancelButton" Content="Отмена" Width="80" Style="{StaticResource ActionButtonStyle}"
                                Click="CancelButton_Click" Visibility="Collapsed"/>
                        <Button x:Name="SaveButton" Content="Сохранить" Width="100" Style="{StaticResource ActionButtonStyle}"
                                Click="SaveButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
